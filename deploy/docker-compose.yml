# name обязателен (иначе конфликт компоузов на сервере)
name: chem

services:
  web:
    image: nginx:alpine
    container_name: chem-web
    restart: unless-stopped
    ports:
      - "127.0.0.1:8090:80"
    volumes:
      - ../site:/usr/share/nginx/html:ro
      - ./nginx-default.conf:/etc/nginx/conf.d/default.conf:ro
